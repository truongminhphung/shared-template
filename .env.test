# Test Environment Configuration
# This file is used for local testing and CI/CD pipelines

# Database Configuration
DB_USER=testuser
DB_PASSWORD=testpassword
DB_NAME=testdb
DB_HOST=localhost
DB_PORT=5432

# Application Configuration
DEBUG=false
APP_NAME=TestApp
